import React from "react";

const Count = (props) => {
  console.log(props.rio);
  console.log("count");
  let count = 0;

  return (
    <div>
      <h1>Count</h1>
    </div>
  );
};

export default Count;
